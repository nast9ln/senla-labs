alter table coffer.person_role add foreign key (person_id) references coffer.person(id);
alter table coffer.person_role add foreign key (role_id) references coffer.role(id);
alter table coffer.advertisement add foreign key (person_id) references coffer.person(id);
alter table coffer.advertisement add foreign key (category_id) references coffer.category(id);
alter table coffer.advertisement add foreign key (top_param_id) references coffer.top_param(id);
alter table coffer.comment add foreign key (advertisement_id) references coffer.advertisement(id);
alter table coffer.comment add foreign key (person_id) references coffer.person(id);
alter table coffer.message add foreign key (sender_id) references coffer.person(id);
alter table coffer.message add foreign key (advertisement_id) references coffer.advertisement(id);
alter table coffer.image add foreign key (advertisement_id) references coffer.advertisement(id);